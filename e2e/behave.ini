[behave]
paths = features
show_skipped = true
show_timings = true
# Output uses behave's default "pretty" formatter on stdout. For CI logs you
# can override per-run with `--format plain` (see run.sh pass-through args).
